{
  "name": "bifrost-workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/pwa"
  ],
  "scripts": {
    "dev": "npm --workspace apps/pwa run dev",
    "build": "npm --workspace apps/pwa run build",
    "preview": "npm --workspace apps/pwa run preview"
  }
}
